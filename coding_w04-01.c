#include <stdio.h>  // include(keyword)

int main() {    // int(keywords) main(Identifiers)
    printf("Hello,"); // printf(Identifier)  ;(Operator)
    printf("C Tokens !");  // printf(Identifier) ;(Operator)
    return 0;  // return(keywords) 0(Constants) ;(Operator)

}